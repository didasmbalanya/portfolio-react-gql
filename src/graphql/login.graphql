mutation Login($email: String!, $password: String!) {
  login(email: $email, password: $password) {
    accessToken
    user {
      id
      title
      description
      firstname
      lastname
      email
      gender
      github
      linkedin
      phone
      phoneAlt
    }
  }
}
